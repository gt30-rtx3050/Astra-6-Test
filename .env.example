# Optional JSON POST endpoints. Leave empty for an explicitly local-only preview.
# Contact payload: { name, phone, email, message }
VITE_CONTACT_ENDPOINT=
# Newsletter payload: { email }
VITE_NEWSLETTER_ENDPOINT=
